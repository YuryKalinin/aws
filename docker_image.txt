user@instructor:~/aws/docker/qrencode$ cat Dockerfile
FROM debian:8

# RUN apt-get install -y python-virtualenv
RUN git clone https://github.com/chubin/qrenco.de
RUN cd qrenco.de
RUN virtualenv ve/
RUN ve/bin/pip install -r requirements.txt 

